/* first trackBar */

// client browser width:
var browserWidth = window.innerWidth || document.documentElement.clientWidth || document.body.clientWidth;

// --------------------------------------------------------------------------------------

// client browser:
var browser = navigator.userAgent;

/*
if (browser.search(/Chrome/) > 0) return 'Google Chrome';
if (browser.search(/Firefox/) > 0) return 'Firefox';
if (browser.search(/Opera/) > 0) return 'Opera';
if (browser.search(/Safari/) > 0) return 'Safari';
if (browser.search(/MSIE/) > 0) return 'Internet Explorer';
*/    

// --------------------------------------------------------------------------------------


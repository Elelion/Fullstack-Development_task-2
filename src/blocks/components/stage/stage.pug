mixin stage
	.stage
		.stage__position
			.stage__range-fill

			form
				.stage__track
					- for (var i = 1; i <= 5; i += 1)
						div(class='stagePoint' + i
							class='stage__track-point'
							class='stage__track-font')= i